<!DOCTYPE html>
<html data-theme="dark">
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
  <%= render "shared/nav" %>
  <!-- 
  <nav class="font-sans flex flex-col text-center sm:flex-row sm:text-left sm:justify-between py-4 px-6 bg-white shadow sm:items-baseline w-full">
    <div class="mb-2 sm:mb-0">
      <%= link_to "Home", root_path, class: "text-2xl no-underline text-primary hover:text-blue-400" %>
      <%= link_to "Create event", new_event_path, class: "text-lg no-underline text-grey-darkest hover:text-blue-dark ml-2" %>
    </div>
    <div id="user_nav">
      <%= link_to "Events", events_path, class: "text-lg no-underline text-grey-darkest hover:text-blue-dark ml-4 mr-4" %>
      <%= link_to "Users", users_path, class: "text-lg no-underline text-grey-darkest hover:text-blue-dark ml-4 mr-4" %>
      <% if logged_in? %>
        Logged in as: <%= current_user.name %>  
        <%= link_to "Log out", log_out_path, class: "text-lg no-underline text-grey-darkest hover:text-blue-dark ml-2" %>
      <% else %>
       <%= link_to "Sign Up", new_user_path, class: "text-lg no-underline text-grey-darkest hover:text-blue-dark ml-2" %>
       <%= link_to "Sign In", log_in_path, class: "text-lg no-underline text-grey-darkest hover:text-blue-dark ml-2" %>
      <% end %>
    </div>
  </nav>
  -->

    <%= yield %>
  </body>
</html>
